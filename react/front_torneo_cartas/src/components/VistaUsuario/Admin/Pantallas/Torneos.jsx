import { Typography } from "@mui/material";

export default function Torneos(){

    return (
        <Typography variant="h1" fontSize={40} fontWeight={"bold"} textAlign={"center"} paddingTop={6} paddingBottom={50}>
            Torneos
        </Typography>
    )
}